질문 및 나름의 답장

프레임 워크의 구조
>

terrain_height 가 아니고 그냥 terrain이 바닥 지형 그러면 terrain_height는 높이 관리인가? 한번 봐야한다

텍스쳐 사용할때 이름 제대로 확인하고 넣자
어제 오크 때도 텍스쳐 이름들로 이상한 헛짓거리 함

뒤에 d%를 사용한 텍스쳐들의 경우에 파일이름은 0부터 시작하는데 실제로 0을 넣으면 벡터 레인지 아웃으로 오류 발생
1부터 시작해야함
> 숫자가 들어가는 것들에 자동으로 1씩 빼주는 코드가 있는지 확인해봐야함

UI창이나 오크같은 같은 객체를 여러개 생성할 때 각기 다른 위치를 넣어주고 싶은데 같은 함수를 타서 똑같이 설정된 vpos를 갖기 떄문에
랜덤 위치를 갖게하는게 아니면 같은 위치에 생성이 된다. 씬스테이지에서 객체 생성을 명령할 때부터 pArg 같은 함수를 통해 넘겨주는 것이
좋을거 같은데 한번 그렇게 고쳐봐야겠다
> 객체에서 컴포넌트를 가지고있을건데 그거를 사용을 해야되나? 결국 그 위치를 설정해주는 것도 밖에서 해줘야되는거 아닌가
> 객체들에 vPos 생성 후 좌표 추가해줌으로써 다른 좌표값을 입력시켰다

그냥 객체 생성할때 객체 생성자 쪽에서 값을 입력시켜주는 방식은 어떤가?

좌표를 넘겨줄때 _vec3 형식의 좌표를 인자에서 void*로 변경해 넘겨주고 사용할때 다시 _vec3 형태로 변경해서 사용하는데
그럴거면 그냥 인자에서 _vec3으로 받아서 사용하면 안되나?
> C++할때도 비슷한걸 본거 같기도 하고 
> 맵툴을 사용해서 객체들의 포지션을 dat 파일로 따로 빼놓았다가 다시 불러오기 때문에 void* 형태로 관리를 하게 된다


각 객체들은 생성 시 좌표가 0,0으로 돼있나?
> 확인 필요


네비 메쉬 생성 시 들어가는 좌표값의 네비메쉬.dat 파일의 구성은? > 또 그걸로 들어가서 실행되는 코드는? 
> 맵툴을 사용해 저장된 객체들의 월드 좌표값이 저장돼있는 것으로 확인됨 

CCell은 무슨 클래스일까. 첫인상은 좌표를 관리하는 클래스?
> 셀에서 네비 메쉬의 크기가 결정된다. 셀 하나가 화면에서 보이는 빨간 삼각형의 정체

>> 네비게이션 메쉬도 크기 설정이 되나? 
> 터레인 cpp에서 네비 메쉬 생성하는 부분 주석처리하니 화면이 평소보다 밝게 켜지면서 화면 역시 회전하지 않음

>>네비게이션 메쉬는 크기 설정을 어디서하나?
>  CCell에서 설정할수 있다

UI 상태 변경 연습 >>