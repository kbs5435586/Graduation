2~3 챕터2 상수와 변수
setw, setf, 확장렬 표기법, get, put 함수

3~4
bool, 문자판별함수 (is,to), #define, const

4~5 
윤수한테 다렉 서버 자문 구함, WSAAsyncSelect를 썼다함
넷겜플 10주차에 찾아봄, 다른 소켓입출력이랑 묶어서 설명한다고 구체적으로
안나와있음, 서버 틱은 타이머 큐로 구현 = 겜서버 들으면 됨
클라에서 스레드 따로 안파고 그냥 WSAAsyncSelect로 리시브받고
send는 일반 함수 사용

5~6
6~7
WSAAsyncSelect 찾아보니까 윈도우 메세지형 서버라고함
찾아보니까 메인에 얘로 리시브해서
이벤트 처리하는 콜백함수내에 WM_SOCKET으로 값 받아서
값에 따라 스위치문으로 다르게 처리해주면 된다는데
서버는 IOCP, 클라에선 어싱크로 통신해주면 된다는게 어느정도 감이 잡힘

7~8 
8~9
9~10
10~11
회의

11~12
용민이한테 계정구함, 윤수한테 서버 프레임웍에 대해서 자문
사실상 서버 구조는 스레드 하나 만들어서 클래스 선언한게 다고
그 외에 필요한 기능들 위주로 프레임웍, 졸작 수준에서는
사실상 서버 프레임웍이라고 할만한게 안나옴
그냥 기능들 클래스 + 서버클래스 정도

12~1
string 문자열(중요)
깔쌈하게 문장 받는법, string str 선언하고 getline(cin, str);
스트링 끼리는 +로 연결가능
substr(), find(), c_str()

총 11시간